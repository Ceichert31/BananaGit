<UserControl
    x:Class="BananaGit.Views.CommitHistoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BananaGit.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:BananaGit.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:GitInfoViewModel}"
    Background="{StaticResource BananaLight}"
    d:DesignHeight="450"
    d:DesignWidth="400"
    mc:Ignorable="d"
    >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            CanContentScroll="True"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            >
            <ItemsControl ItemsSource="{Binding CommitHistory}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="GitHistoryItemBorder" BorderBrush="{StaticResource BananaGradient}" BorderThickness="0,3,0,0">
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <Image 
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        Margin="0,-10,0,0" 
                                        Style="{StaticResource GitBananaIconStyle}"/>
                                    <Label
                                        Content="{Binding Message}"
                                        FontSize="12"
                                        FontWeight="Bold"
                                        Foreground="Black"
                                    />
                                </StackPanel>
                                <StackPanel Margin="0,-15,0,0" Orientation="Horizontal">
                                    <Label
                                        Content="{Binding Author}"
                                        FontSize="12"
                                        Foreground="Black"
                                        />
                                    <Label
                                        Content="{Binding Date}"
                                        FontSize="12"
                                        Foreground="Black"
                                        />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource PreviousData}}" Value="{x:Null}">
                                <Setter TargetName="GitHistoryItemBorder" Property="BorderThickness" Value="0,0,0,0"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
