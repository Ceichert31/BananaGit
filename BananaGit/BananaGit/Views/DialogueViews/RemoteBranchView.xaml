<Window
    x:Class="BananaGit.Views.DialogueViews.RemoteBranchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BananaGit.Views.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:BananaGit.ViewModels"
    Title="RemoteBranchView"
    Width="500"
    Height="400"
    d:DataContext="{d:DesignInstance Type=viewmodels:RemoteBranchViewModel}"
    Background="{StaticResource BananaYellow}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label
            Background="{StaticResource BananaAccent}"
            Content="Checkout Remote Branches"
            FontWeight="Bold"
            Foreground="Black" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ItemsSource="{Binding RemoteBranches}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:BranchControl DataContext="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Label
            Grid.Row="2"
            Background="{StaticResource BananaAccent}"
            Content="Manage Local Branches"
            FontWeight="Bold"
            Foreground="Black" />

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ItemsSource="{Binding LocalBranches}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:BranchControl DataContext="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>