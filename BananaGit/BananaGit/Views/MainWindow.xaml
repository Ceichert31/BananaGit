<Window
    x:Class="BananaGit.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BananaGit.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:BananaGit.ViewModels"
    xmlns:views="clr-namespace:BananaGit.Views"
    Title="BananaGit"
    Width="1200"
    Height="700"
    MinWidth="450"
    MinHeight="450"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
    Background="{StaticResource BananaYellow}"
    Icon="../Assets/Banana.ico"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="1.2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="1.5*" />
        </Grid.ColumnDefinitions>

        <!--  Toolbar  -->
        <local:ToolbarView
            Grid.Row="0"
            Grid.ColumnSpan="4"
            Margin="0,-1,0,0"
            DataContext="{Binding ToolbarViewModel}"
            />

        <!--  Git Changes  -->
        <local:GitChangesView
            Grid.Row="1"
            Grid.RowSpan="2"
            Grid.Column="3"
            DataContext="{Binding GitInfoViewModel}"
            />

        <!--  Git Commit  -->
        <local:CommitView
            Grid.Row="3"
            Grid.Column="3"
            DataContext="{Binding CommitViewModel}"
            />

        <!--  Commit History & Tutorial  -->
        <ContentControl
            Grid.Row="1"
            Grid.RowSpan="3"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Content="{Binding}"
            >
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate" Value="{StaticResource CommitHistoryTemplate}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ToolbarViewModel.IsTutorialOpen}" Value="True">
                            <Setter Property="ContentTemplate" Value="{StaticResource TutorialTemplate}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>

        <!--  Render first to avoid overlap  -->
        <Image
            Grid.Row="1"
            Grid.RowSpan="3"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="-10,0,0,0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            IsHitTestVisible="False"
            Opacity="0.2"
            Source="pack://application:,,,/Assets/Banana.png"
            />

        <GridSplitter
            Grid.Row="1"
            Grid.RowSpan="3"
            Grid.Column="2"
            Width="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch"
            Background="White"
            />
    </Grid>
</Window>
